Node.js / JavaScript Backend Setup

Create project:

mkdir backend
cd backend

Initialize:

npm init -y

Install Express:

npm install express

Install CORS:

npm install cors

Install dotenv:

npm install dotenv

Start server:

node server.js

Development mode:

npm install nodemon --save-dev
npx nodemon server.js